<section class="container my-5" style="height: fit-content; min-height: 85vh; overflow-y: scroll;">
    <h1 class="m-3">Editar Utilizador</h1>
    <div class="row container">
        <div class="col-12 col-sm-6">
            {{#if client}}
            <form action="/edit/client/{{client.id}}" method="post">
                <div class="form-group">
                    <label for="name">Nome</label>
                    <input class="form-control" type="text" name="name" id="name-input" value="{{client.nome}}">
                </div>
                <div class="form-group">
                    <label for="lastName">Apelido</label>
                    <input class="form-control" type="text" name="lastName" id="lastName-input" value="{{client.apelido}}">
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input class="form-control" type="email" name="email" id="email-input" value="{{client.email}}">
                </div>
                <div class="form-group">
                    <label for="company">Organização</label>
                    <input class="form-control" type="text" name="company" id="company-input" value="{{client.company}}">
                </div>
                <button class="btn btn-warning" type="submit">Editar</button>
            </form>
            {{else}}
            <h1 class="text-center">Client Not Found</h1>
            {{/if}}
        </div>
        <div class="my-5 col-12 col-sm-6 d-flex justify-content-center align-content-center flex-column">
            <h4>Nome, Apelido e Email</h4>
            <p>É o nome e apelido da peso responsavel na organização pela parceria com a Good To Recycle</p>
            <h4>Organização</h4>
            <p>É o nome da organização parceira</p>
        </div>
    </div>
</section>